# Sidebar pagination and Priority

Captured on 17 September 2026 from the production `WorkspaceSidebar` and
`useSidebarHistory` controller in a local browser fixture. All workspace names
and conversations are synthetic; no application accounts or transcripts were
loaded. These PNGs are direct sidebar crops without visual edits.

- `chats.png`: 312px sidebar, five conversations per workspace and Show more.
- `priority.png`: 312px sidebar, inline workspace labels and all three date groups.
- `priority-narrow.png`: 240px sidebar, independent workspace/title truncation.

Browser checks also exercised five-to-ten pagination, retained counts after
switching modes, cross-workspace selection, and the 40% workspace-prefix cap
without horizontal overflow.
